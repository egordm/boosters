[package]
name = "boosters-python"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[lib]
name = "boosters_python"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }

