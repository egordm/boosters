name: Python

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Build & Test
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python: ["3.12", "3.13"]

    defaults:
      run:
        shell: bash

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true

      - name: Set up Python
        run: uv python install ${{ matrix.python }}

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Install dependencies
        run: |
          uv sync --group dev

      - name: Run full workspace checks
        run: uv run poe check

  stub-freshness:
    name: Check Stub Freshness
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true

      - name: Set up Python
        run: uv python install 3.12

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Install dependencies
        run: |
          uv sync
          uv pip install maturin

      - name: Build Python bindings
        run: |
          cd packages/boosters-python
          uv run maturin develop

      # TODO: Uncomment when stub generation is automated
      # - name: Generate stubs
      #   run: |
      #     cd packages/boosters-python
      #     uv run python -c "import boosters; boosters.generate_stubs()"
      #
      # - name: Check stub freshness
      #   run: |
      #     if ! git diff --exit-code packages/boosters-python/python/boosters/_boosters_rs.pyi; then
      #       echo "::error::Type stubs are out of date. Run stub generation and commit the changes."
      #       exit 1
      #     fi

      - name: Verify stubs exist
        run: |
          if [ ! -f packages/boosters-python/python/boosters/_boosters_rs.pyi ]; then
            echo "::error::Type stub file is missing"
            exit 1
          fi
