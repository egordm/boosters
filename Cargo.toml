[package]
name = "xgboost-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"
thiserror = "1.0"
serde_with = { version = "3.16.0", features = ["json"] }

[dev-dependencies]
rstest = "0.21"

